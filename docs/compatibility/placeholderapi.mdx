---
title: "🔤 PlaceholderAPI"
description: "PlaceholderAPI integration for Oraxen."
---

import { Callout, Tabs, Tab, Steps } from '@site/src/components/Compat'

# 🔤 PlaceholderAPI

## Overview
Oraxen registers several PlaceholderAPI placeholders. This allows you to reference glyphs, shifts or the pack hash dynamically via placeholders.

## Usage
`%oraxen_yourglyph%` - Basic usage.<br />
`%oraxen_shift_N%` - Shifts N pixels to the right (positive).<br />
`%oraxen_neg_shift_N%` - Shifts N pixels to the left (negative).<br />
`%oraxen_pack_url%` - The resource pack URL.<br />
`%oraxen_pack_hash%` - The pack SHA-1 hash.<br />
`%oraxen_hash%` - Alias for `%oraxen_pack_hash%`. Resolved after glyph lookup, so a glyph named `hash` takes precedence.<br />
---
title: "✨ Text Effects"
description: "Creating & using text effects in Oraxen."
asIndexPage: true
---

import Highlight from '../../../components/Highlight.js'
import { Callout } from 'nextra/components'

# ✨ Text Effects

## Overview
Text effects are shader-based visual effects that can be applied to any text in Minecraft. Unlike animated glyphs which swap between sprite frames, text effects modify how existing characters render (color, position, opacity) using GLSL shaders.

<Callout type="warning">
    Text Effects require Minecraft 1.21.4 or higher. ViaVersion for TextEffects is only partially supported.
</Callout>

## Configuration
Text effects are configured in the `text_effects.yml` file in the `plugins/Oraxen/` directory.

<PluginFileTree
  initialTreeData={[
    {
      id: "plugins",
      name: "plugins",
      children: [
        {
          id: "plugins/Oraxen",
          name: "Oraxen",
          children: [
            {
              id: "plugins/Oraxen/text_effects",
              name: "text_effects.yml",
            },
            {
              id: "plugins/Oraxen/settings",
              name: "settings.yml",
            }
          ]
        }
      ]
    }
  ]}
/>

### Settings

```yaml
TextEffects:
  enabled: true                    # Master toggle for all text effects
  shader:
    template: auto                 # auto, effects_only, animated_only
  effects:                         # Optional: override enabled state per effect
    rainbow:
      enabled: true
    wave:
      enabled: false               # Disable specific effects
```

### Properties
Effects are defined with their visual behavior baked directly into the GLSL snippets.

```yaml
version: 1
 
# Shared GLSL code available to all effects
shared:
  fragment_prelude: |
    vec3 hsv2rgb(vec3 c) {
        vec4 K = vec4(1.0, 2.0/3.0, 1.0/3.0, 3.0);
        vec3 p = abs(fract(c.xxx + K.xyz) * 6.0 - K.www);
        return c.z * mix(K.xxx, clamp(p - K.xxx, 0.0, 1.0), c.y);
    }
 
effects:
  rainbow:
    # id: 0                        # Optional - auto-assigned if omitted
    enabled: true
    # trigger_color: "#FD0D00"     # Optional - defaults to #FDxD00
    description: Cycles through rainbow colors over time
    snippets:
      - fragment: |
          float hue = fract(charIndex * 0.03 + timeSeconds * 0.09);
          texColor.rgb = hsv2rgb(vec3(hue, 0.9, 1.0));
```

## Usage
<details>
<summary><strong>💬 MiniMessage</strong></summary>

`<effect:rainbow>This Text is Rainbow.</effect>` - Applies a rainbow effect to the text.<br />
`<effect:wave>Wavy text here</effect>` - Applies a wave effect to the text.<br />
`<effect:shake>Shaking text!</effect>` - Applies a shake effect to the text.<br />

</details>
